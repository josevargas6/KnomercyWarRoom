# Replay results

Deterministic runner outputs paired with `tests/replays/` and
`tests/golden/`. Results are generated artifacts and must be regenerated when
the replay corpus or runner contract changes.
